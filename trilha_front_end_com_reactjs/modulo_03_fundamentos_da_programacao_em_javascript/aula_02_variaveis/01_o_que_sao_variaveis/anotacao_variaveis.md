# VARIÁVEIS
* Podemos definir como um espaço que será alocado na memória do computador, com finalidade de armazenar determinada informação. Como o próprio nome diz "variável" sugere que tais informações podem variar, ou seja, a cada vez que o sistema ou rotina for executada, o valor armazenado poderá ser diferente.
